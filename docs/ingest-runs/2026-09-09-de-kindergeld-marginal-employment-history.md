# SGB IV §8 applicable in2025

The official DRV archive identifies the text as effective1 March2024 through31 December2025. Native HTML anchor-range extraction retains all operative paragraphs(1), (1a), (1b), (2), (2a), and(3). Its2707-character body equals the complete operative content after whitespace normalization. The source HTML and its SHA are retained; the manifest separately records the displayed validity interval and capture date.

This historical text has the three-month/70-workday short-term employment limit. The current consolidation also includes an agricultural extension, so it cannot be substituted silently for2025. The historical row uses a version-qualified citation and does not overwrite the current SGB IV corpus text.

Reproduction: run native `extract-official-documents` with `manifests/de-kindergeld-marginal-employment-history.yaml`, base `data/corpus`, version `2026-09-09-de-kindergeld-marginal-employment-history`. The recorded run uses the supported `local_path` replay of the exact downloaded official HTML with its declared snapshot SHA. This changes transport only.

Together with MiLoG and MiLoV5, the final additive release selector has40 scopes, preserving all37 preceding scopes. No historical applicability is inferred merely from capture date. No certification or serving activation is performed.
