Added `corpus.provision_anchors`, a derived, rebuildable leaf-level annotation layer (schema + local JSONL under `data/corpus/anchors/` + `generate-anchors`/`resolve-anchor`/`load-anchors-supabase` CLI), populated for `7 CFR 273.9` (so `273.9(d)(6)(iii)` is addressable) and `us-ma 106 CMR 365.180(A)`; every anchor is verified byte-equal to its parent provision slice with the printed label at the span head, and the leaf citation path is the stable key.
