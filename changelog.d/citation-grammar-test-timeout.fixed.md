Stop the corpus-wide tests from timing out in CI. `scripts/validate_citation_paths.py` still parses every provision row but keeps only the five fields its checks read and precompiles its irregular-family predicates; its output on the committed corpus is byte-identical, while the scan's peak RSS drops from about 6 GB to about 0.45 GB in local runs. The three tests that parse the whole provision corpus (the citation-path grammar module, the provision identity-convention check and the claims evidence-span check) now take their pytest-timeout budget from `tests/corpus_scan.py`, which scales with the committed provision JSONL size (1 s per 10 MB, never below the suite's 60 s), instead of CI's flat `--timeout=60`, which had errored all eight corpus-dependent grammar tests on slower runners (axiom-corpus#736).
