Replay Wisconsin Statutes reruns strictly from retained source bytes: a `source_dir` that misses a file now fails instead of re-downloading the live page, and the chapter 71 recovery manifest points `source_dir` at the retained scope directory it had been missing and pins `include_subunits: false` and the new `include_publication_note: false`, so its rerun reproduces the signed 2026-07-16-pit-west-chapter-71 artifacts byte for byte.
