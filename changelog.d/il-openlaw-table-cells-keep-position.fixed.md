An empty table cell in an OpenLaw page is now emitted as an empty field in its column instead of being dropped, so the two totals rows of the National Insurance Law's contribution schedule (לוח י׳, `schedule-j/sign-1`) keep the data rows' eleven columns and 14.50 sits under the rate column rather than the insurance-branch column. A row is dropped only when every cell in it is empty. Five rows change; regressions cover a leading blank cell and the committed totals.
