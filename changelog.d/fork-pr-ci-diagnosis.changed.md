Name the cause when CI runs without `AXIOM_CORPUS_INGEST_PUBLIC_KEY`: a new first step in the `test` job emits an error annotation and a step summary naming the cause (a fork-head pull request run does not receive this repository's Actions variable; a same-repository run has it misconfigured) and, for fork heads, pointing to CONTRIBUTING.md, which now tells org members to push same-repo branches, not forks; AGENTS.md and CLAUDE.md carry the same note. The authentication-gate tests are unchanged and still fail in that context (axiom-corpus#600, #606).
