Repaired the locally-repairable release-validation debt: derived canonical inventories and recomputed coverage for 55 scopes whose provisions exist locally but whose artifacts were missing or stale, cleared three dangling NYC admin-code parent references, and added a `validate-release` CI job that runs on push/cron and arms full R2-backed validation automatically once R2 secrets are configured. Deep validation now reports zero errors outside the 69 state-statute scopes whose artifacts live only in R2.
