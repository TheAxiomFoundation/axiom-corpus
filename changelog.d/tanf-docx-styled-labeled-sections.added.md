Add the `styled_labeled_sections` DOCX segmentation mode to the official-documents extractor: sections start only at heading-styled paragraphs (or body paragraphs matching `heading_text_pattern` when `heading_paragraphs_only` is false), never at table rows; `(Continued)` restatements at page breaks merge into the first section. Used for the CDSS EAS TANF manual, whose plain `labeled_sections` extraction produced duplicate citation paths.
