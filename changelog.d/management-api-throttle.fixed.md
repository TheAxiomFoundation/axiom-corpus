Registering a large signed release object no longer fails on the Supabase Management API's per-minute request budget. The 761-scope `us-rulespec-2026-09-13-federal-and-plans-union` publish (run 34793779840) completed publication in 36 minutes, then `stage_release_object.py` was throttled (`ThrottlerException: Too Many Requests`) while uploading the object's ~70 128 KB chunks as one request each. Management API requests now wait out a throttle response with backoff (15, 30, 60, 60, 60 s) and retry, chunk uploads are paced 1.5 s apart, and the `publish` workflow retains the signed release object as a run artifact even when registration fails after publication.