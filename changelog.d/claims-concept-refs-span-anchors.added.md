Additively enriched source claims (Phase A, item A4) so the claims layer survives rule renames: each claim now carries a `concept` cross-reference into the concept registry (`axiom-corpus/concept-registry/v1`) and each evidence item carries a release-`r0`-pinned `span` anchor (verified char offsets plus provision-body SHA-256) alongside the existing `text_contains` selector. Claim `id`, `subject`, and existing evidence fields are unchanged, so `axiom-encode`'s proof-validation gate is unaffected. Added `docs/claims-format.md` (current format, additive fields, and the deferred subject-re-key coordinated change) and extended `tests/test_claims.py` to re-extract every span to its selector and resolve every concept reference in the registry.
