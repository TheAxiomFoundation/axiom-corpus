Additively enriched source claims (Phase A, item A4) so the claims layer survives rule renames: each claim now carries a `concept` cross-reference into the concept registry (`axiom-corpus/concept-registry/v1`) and each evidence item carries a verified char-offset `span` anchor plus provision-body SHA-256 alongside the existing `text_contains` selector. Existing `r0` values are historical claim-anchor labels, not v2 corpus release identities, and must migrate to a separate signed claims boundary before production use. Claim `id`, `subject`, and existing evidence fields are unchanged. Added `docs/claims-format.md` and extended `tests/test_claims.py` to re-extract every span and resolve every concept reference.
